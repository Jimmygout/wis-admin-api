{% extends "base.html.twig" %}

{% block title %}Admin. | {{ parent() }}{% endblock %}

{% block content %}

<div class="row">
    <div class="col s12">
      <ul class="tabs">
        <li class="tab col s3"><a class="active" href="#concert">Concerts</a></li>
        <li class="tab col s3"><a href="#alert">Alertes</a></li>
        <li class="tab col s3 disabled"><a href="#test3">Disabled Tab</a></li>
        <li class="tab col s3"><a href="#test4">Test 4</a></li>
      </ul>
    </div>
    <div id="concert" class="col s12">
    {% include "concert/index.html.twig" %}
    </div>
    <div id="alert" class="col s12">
    {% include "concert/index.html.twig" %}
    </div>
    <div id="test3" class="col s12">Test 3</div>
    <div id="test4" class="col s12">Test 4</div>
  </div>



{#
    <div class="admin-page">
        <div class="container">
            <div class="row">
                <div class="col s12">
                    <a href="{{ path('article_add') }}" class="btn btn blue darken-4" style="float: right">Ajouter</a>
                    <h5>Articles</h5>
                    <table>
                        <tr>
                            <th>#</th>
							<th>image</th>
                            <th>Titre</th>
                            <th>Publier</th>
                            <th>Date de publication</th>
                            <th>Date de modification</th>
                            <th>Actions</th>
                        </tr>
                        {% set i = 1 %}
                        {% for article in articles %}
                            <tr>
							
						       <td>{{ i }}</td>
							   	<td style="padding:0px 0px 0px 0px"><img style="width: 3.5rem;" src="{{ asset('uploads/' ~ article.picture) }}" class="responsive-img"></td>
                         
                                <td><a href="{{ path('article_show', {'url': article.url}) }}" target="_blank">{{ article.title }}</a></td>
                                <td>
                                    <span class="badge {{ article.isPublished ? 'blue' : 'red' }}">
                                        {{ article.isPublished ? 'oui' : 'non' }}
                                    </span>
                                </td>
                                <td>{{ article.isPublished ? article.publicationDate|date('d/m/Y') : '-' }}</td>
                                <td>{{ article.lastUpdateDate|date('d/m/Y') }}</td>
                                <td>
                                    <a href="{{ path('article_edit', {'id': article.id}) }}"><i class="material-icons blue-text">edit</i></a>
                                    <a href="{{ path('article_remove', {'id': article.id}) }}"><i class="material-icons red-text">delete</i></a>
                                </td>
                            </tr>
                            {% set i = i+1 %}
                        {% endfor %}
                    </table>
                </div>
            </div>

            <div class="row">
                <div class="col s12 m-t-30">
                    <h5>Utilisateurs</h5>
                    <table>
                        <tr>
                            <th>#</th>
                            <th>Adresse email</th>
                            <th>Username</th>
                            <th>Roles</th>
                            <th>Actions</th>
                        </tr>
                        {% set i = 1 %}
                        {% for user in users %}
                            <tr>
                                <td>{{ i }}</td>
                                <td>{{ user.email }}</td>
                                <td>{{ user.username }}</td>
                                <td>
                                    <span class="badge blue">
                                        admin.
                                    </span>
                                </td>
                                <td>
                                    <a href=""><i class="material-icons red-text">delete</i></a>
                                </td>
                            </tr>
                            {% set i = i+1 %}
                        {% endfor %}
                    </table>
                </div>
            </div>

            <div class="row">
                <div class="col s12 m-t-30">
                        <a href="{{ path('alert_new') }}" class="btn btn blue darken-4" style="float: right">Ajouter</a>
                    <h5>Les alerts</h5>
                    <table>
                        <tr>
                            <th>#</th>
                            <th>Désignation</th>
                            <th>Publier</th>
                            <th>Date de debut</th>
                            <th>Date de fin</th>
                        </tr>
                        {% set i = 1 %}
                        {% for alert in alerts %}
                            <tr>
                                <td>{{ i }}</td>
                                <td>{{ alert.designation }}</td>
                                <td>
                                    <span class="badge {{ alert.publier ? 'blue' : 'red' }}">
                                        {{ alert.publier ? 'oui' : 'non' }}
                                    </span>
                                </td>
                                <td>{{ alert.dateDebut|date('d/m/Y H:i:s') }}</td>
                                <td>{{ alert.DateFin|date('d/m/Y H:i:s') }}</td>
                                <td>
                                    <a href="{{ path('alert_edit', {'id': alert.id}) }}"><i class="material-icons blue-text">edit</i></a>
                                    <a href="{{ path('alert_delete', {'id': alert.id}) }}"><i class="material-icons red-text">delete</i></a>
                                 </td>
                            </tr>
                            {% set i = i+1 %}
                        {% endfor %}
                    </table>
                </div>
            </div>

            <div class="row">
                <div class="col s12 m-t-30">
                        <a href="{{ path('concert_new') }}" class="btn btn blue darken-4" style="float: right">Ajouter</a>
                    <h5>Les Concerts</h5>
                    <table>
                        <tr>
                            <th>#</th>
                            <th>Chanteur</th>
                            <th>Désignation</th>
                            <th>Date concert</th>
                            <th>Image</th>
                        </tr>
                        {% set i = 1 %}
                        {% for concert in concerts %}
                            <tr>
                                <td>{{ i }}</td>
                                <td>
                                    <span class="badge {{ concert.publier ? 'blue' : 'red' }}">
                                        {{ concert.publier ? 'oui' : 'non' }}
                                    </span>
                                </td>
                                <td>{{ concert.dateConcert|date('d/m/Y') }}</td>
                                <td>
                                    <a href="{{ path('concert_edit', {'id': concert.id}) }}"><i class="material-icons blue-text">edit</i></a>
                                    <a href="{{ path('concert_delete', {'id': concert.id}) }}"><i class="material-icons red-text">delete</i></a>
                                </td>
                                <td style="padding:0px 0px 0px 0px"><img style="width: 3.5rem;" src="{{ asset('uploads/' ~ concert.picture) }}" class=""></td>
                         
                            </tr>
                            {% set i = i+1 %}
                        {% endfor %}
                    </table>
                </div>
            </div>


        </div>
    </div>
#}
{% endblock %}
